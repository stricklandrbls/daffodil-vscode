import * as assert from 'assert'
import * as manager from '../manager'

describe('Data Editor Extension Manager', () => {
  describe('Instantiation of Manager', () => {
    it('Should maintain a record of active data editors', () => {})
    it('Should maintain Omega Edit server heartbeats', () => {})
  })
})
