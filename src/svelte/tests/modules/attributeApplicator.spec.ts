import {
  type AttributeApplicator,
  type Attribute,
} from '../../src/utilities/ByteCategories/BitAttribute.ts'
import { describe, it } from 'mocha'
import assert from 'assert'
import {
  BitApplicator,
  type AttributeRange,
} from '../../src/utilities/ByteCategories/ViewportBitAttributes.ts'

describe('Attribute Applicator', () => {
  it('should apply a specified attribute to an explicit type', () => {})
})
